version: "3.3"
services:
  biiboom_etl:
    build: .
    container_name: biiboom-etl
    platform: linux/amd64
    restart: always
    networks:
      - biiboom
    environment:
      env: local
      pgsql_ip: host.docker.internal
networks:
  biiboom:
