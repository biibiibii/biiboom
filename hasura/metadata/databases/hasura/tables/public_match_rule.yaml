table:
  name: match_rule
  schema: public
array_relationships:
  - name: sites
    using:
      foreign_key_constraint_on:
        column: rule_id
        table:
          name: site
          schema: public
